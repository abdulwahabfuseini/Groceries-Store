import { TestimonialProps } from '@/contexts/Types'
import React from 'react'

const TestimonialCard = ({name, email,  image, message}: TestimonialProps) => {
  return (
    <div>TestimonialCard</div>
  )
}

export default TestimonialCard